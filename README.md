# Rust-IT-Group3

今回作るもの→「メッセージが一定時間で消える掲示板」

・詳細

サーバーを立ててそこにユーザーが自由に書き込みができる

時間経過で書き込んだメッセージが利用するサーバーから消える(見えなくなる)

消えたメッセージは管理者のみ見れるように別なサーバーを立ててそこに送る

サーバー内に複数のスレッドを作る

スレッド自体は時間経過でも消えないがスレッドの中身は全部消える

ユーザーにID等は付与しない
